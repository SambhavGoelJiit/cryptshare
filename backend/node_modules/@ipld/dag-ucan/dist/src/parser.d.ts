export function parse<C extends UCAN.Capabilities>(jwt: string | UCAN.JWT<C>): UCAN.Model<C>;
export function parseHeader(header: string): {
    typ: "JWT";
    ucv: `${number}.${number}.${number}`;
    alg: string;
};
export function parsePayload<C extends UCAN.Capabilities>(source: string): UCAN.Payload<C>;
import * as UCAN from "./ucan.js";
//# sourceMappingURL=parser.d.ts.map